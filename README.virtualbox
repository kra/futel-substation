# Set up vagrant:

have vagrant, virtualbox, ansible
vagrant plugin install vagrant-vbguest

# Deploy mechaoperator to virtualbox with vagrant:

vagrant up mechaoperator

# Deploy backupbox to virtualbox with vagrant:

vagrant up backupbox
